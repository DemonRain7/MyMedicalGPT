===========================================
MyMedicalGPT - 使用指南
===========================================

✅ 项目已创建完成！

📁 项目位置: 
   d:\CODE\codes\MedicalGPT\MedicalGPT\MyMedicalGPT\

📚 文档导航:
   1. QUICKSTART.md      - 5分钟快速上手（新手必读）
   2. README.md          - 完整项目文档
   3. INFERENCE_GUIDE.md - 推理技术详解
   4. PROJECT_SUMMARY.md - 项目总结与对比

🚀 快速开始三步走:

   步骤1: 安装依赖
   ----------------
   cd MyMedicalGPT
   pip install -r requirements.txt

   步骤2: 训练第一个模型（选择一种）
   ---------------------------------
   # 方式A: 只训练SFT（推荐新手，20-30分钟）
   bash scripts/train_sft.sh
   python merge_peft_adapter.py --base_model Qwen/Qwen2.5-0.5B-Instruct --lora_model outputs-sft --output_dir my-model

   # 方式B: 完整流程（PT+SFT+DPO，1-2小时）
   bash scripts/run_pipeline.sh

   步骤3: 测试推理
   ----------------
   # 命令行交互
   python inference.py --base_model my-model --template_name qwen --interactive

   # 或Web界面（需要先安装：pip install gradio）
   python inference_gradio.py --model_path my-model

🎯 核心功能:

   训练:
   - ✅ 增量预训练 (PT)
   - ✅ 有监督微调 (SFT) 
   - ✅ 直接偏好优化 (DPO)
   - ✅ LoRA/QLoRA高效微调
   - ✅ 多卡并行训练

   推理:
   - ✅ 命令行交互式对话
   - ✅ 批量推理
   - ✅ FastAPI服务
   - ✅ Gradio Web界面
   - ✅ INT8/INT4量化
   - ✅ Flash Attention加速

📊 项目结构:

   MyMedicalGPT/
   ├── 训练脚本: pretraining.py, supervised_finetuning.py, dpo_training.py
   ├── 推理脚本: inference.py, inference_api.py, inference_gradio.py
   ├── 工具脚本: merge_peft_adapter.py, template.py
   ├── 启动脚本: scripts/*.sh
   ├── 示例数据: data/pretrain, data/finetune, data/reward
   └── 文档: *.md

💡 常用命令速查:

   # 查看训练日志
   tensorboard --logdir outputs-sft/runs --port 6006

   # 合并LoRA权重
   python merge_peft_adapter.py --base_model <base> --lora_model <lora> --output_dir <output>

   # API服务部署
   python inference_api.py --model_path my-model --port 8000

   # 批量推理
   python inference.py --base_model my-model --data_file input.jsonl --output_file output.jsonl

🔥 高级推理方案:

   # vLLM高性能部署（需要先安装：pip install vllm）
   python -m vllm.entrypoints.openai.api_server --model my-model --port 8000

   # INT8量化推理
   python inference.py --base_model my-model --load_in_8bit

❓ 遇到问题？

   1. 显存不足: 减小batch_size，启用gradient_checkpointing
   2. 训练太慢: 减小数据量，减少epoch，使用更小模型
   3. 效果不好: 增加数据量，提高训练轮数，检查数据质量
   4. 更多FAQ: 查看README.md

📞 获取帮助:

   - HuggingFace文档: https://huggingface.co/docs
   - Transformers教程: https://huggingface.co/course

祝你训练愉快！🎉
